# Progress — WhatsApp Gateway SaaS

> Living checklist. Read this together with `CLAUDE.md` at the start of every session.
> Update it at the end of every milestone (tick the box, add notes, set "Next step").
> Last updated: 2026-09-23

## Where we are

**All 8 roadmap milestones (M0-M8) are done and live-verified, and all committed by the owner.** Post-roadmap work, beyond `CLAUDE.md`'s original scope, per the owner's direction — **all of the below is now committed** (verified against `git log`: `76a0592` solve bugs of connecting again, `4dc7111` forgot password, `bd3e5d1` cashfree account and billing module phase 1, `0a7eb33` account module, `0b69621` change token size from 64 to 32 characters):
1. **Hardening fixes + "Send a test message" button** — coded, tested, committed.
2. **Password reset** — coded, fully tested, committed.
3. **Billing (Cashfree)** — built, confirmed working against the real sandbox API (real plans, a real subscription + checkout completed live), including a real local PHP CA-bundle fix along the way. Checkout works; automatic webhook status confirmation is **deliberately deferred** (needs a public URL — ngrok or real deployment — the owner chose to move on rather than set that up now). Committed.
4. **Queues + webhook retries** — webhook deliveries (M8, incoming-message → customer webhook) now go through a queued, retried job instead of one synchronous best-effort attempt. Message *sending* deliberately stays synchronous (queuing it would break the public API's immediate-response contract — flagged, not done without a separate explicit decision). Committed.
5. **Change password + delete account** — coded and tested. 2FA / audit logs / admin visibility explicitly deferred as their own future milestone(s), owner's choice. Committed.
6. **Token size tweak** — `ApiToken::generateFor()` changed from `Str::random(64)` to `Str::random(32)` (`0b69621`), not otherwise documented below since it's a one-line change.

**Working tree is clean; `main` is up to date with `origin/main`.** Whether the live-verification checklist further down (queued webhooks actually delivering, boot-reconnect, plan limits, etc.) was clicked through by the owner before these commits is not confirmed by git alone — treat those items as still open unless/until confirmed.

**Next up: an API Docs page** (owner's stated next step).

## Milestones

- [x] **M0 — Environment & plan** (versions locked in `CLAUDE.md` section 3)
- [x] **M1 — Laravel bootstrap**
  - [x] Git repo at the root, root `.gitignore`
  - [x] Empty MySQL/MariaDB database `whatsapp_gateway`
  - [x] Laravel 13.x in `backend/` (no starter kit, MySQL driver, PHPUnit)
  - [x] Default migrations run (9 tables: users, password_reset_tokens, sessions, cache, cache_locks, jobs, job_batches, failed_jobs, migrations)
- [x] **M2 — Auth** (register, login, logout)
  - [x] Bootstrap 5.3.8 + `@popperjs/core` 2.11.8 installed via npm; Tailwind packages removed
  - [x] `RegisterController` and `LoginController` (`app/Http/Controllers/Auth/`)
  - [x] Password hashing, CSRF, session regeneration on login, session invalidation on logout
  - [x] Login rate limit: 5 failed attempts per email+IP, then 60 s lock
  - [x] Same error message for wrong email and wrong password (no account enumeration)
  - [x] Views: `layouts/app`, `auth/login`, `auth/register`
  - [x] `tests/Feature/AuthTest.php` (18 tests)
- [x] **M3 — Dashboard**
  - [x] `DashboardController` (single-action) + `/dashboard` route
  - [x] `/` redirects to `/dashboard` (guests then go on to `/login`)
  - [x] Real navbar (Dashboard active; Instances and API Docs are greyed-out "soon" placeholders)
  - [x] Dashboard cards: Instances = 0, "No instances yet", Account details; "Getting started" checklist
  - [x] `tests/Feature/DashboardTest.php` (8 tests); `ExampleTest` updated for the redirect
  - [x] Deleted the unused `welcome.blade.php` (it contained inlined Tailwind CSS)
  - Note: `instanceCount` / `connectedCount` in `DashboardController` are hard-coded to 0 until M5.
- [x] **M4 — Worker skeleton**
  - [x] `whatsapp-worker/` created: Node/TS, ESM (`"type": "module"`), Fastify, Zod, Vitest
  - [x] Packages: `fastify`, `zod` (deps); `typescript`, `tsx`, `vitest`, `@types/node`, `pino-pretty` (dev). No `dotenv` — Node's native `--env-file` loads `.env`. Fastify's built-in logger is Pino already, no separate `pino` package needed.
  - [x] `src/config.ts` — Zod-validated env (`HOST`, `PORT`, `INTERNAL_API_SECRET`; refuses to start without a secret ≥16 chars)
  - [x] `src/auth.ts` — `X-Internal-Secret` check, constant-time (SHA-256 the two values, then `crypto.timingSafeEqual` on the fixed-length digests — avoids both the length-mismatch throw and a length side-channel)
  - [x] `src/app.ts` builds the Fastify instance (testable via `.inject()`, no real port needed); `src/index.ts` is the thin entry point that calls `.listen()`
  - [x] Routes: `GET /health` (public), `GET /internal/ping` (protected demo route, proves the auth hook works — will be replaced by real endpoints from M5)
  - [x] `tests/health.test.ts`, `tests/auth.test.ts` — 4 tests, all passing; `npm run typecheck` clean
  - [x] Manually smoke-tested with `npm run dev` + `curl`: `/health` → 200, `/internal/ping` with no/wrong secret → 401, with correct secret → 200
  - [x] `.env` (real random secret, local only, git-ignored) + `.env.example` (documents the vars, no secret)
  - Installed versions: `fastify` ^5.12.5, `zod` ^4.6.5, `typescript` ^7.0.2, `tsx` ^4.23.15, `vitest` ^5.0.1, `@types/node` ^26.6.2, `pino-pretty` ^13.1.3.
- [x] **M5 — Instance + QR** *(verified live — owner scanned a real QR and reached Connected)*
  - **Backend (`backend/`):**
    - [x] `whatsapp_sessions` migration/table: `instance_id` (UUID, unique, public), `user_id` FK cascade-delete, `name`, `status` (`connecting`/`qr_pending`/`connected`/`disconnected`/`logged_out`), `qr_code`, `qr_updated_at`, `phone_number`, `connected_at`, `last_disconnect_reason`
    - [x] `WhatsappSession` model — route-bound by `instance_id` (never the internal id), auto-generates the UUID on creating; `User::whatsappSessions()` relation; `WhatsappSessionFactory` (with a `connected()` state) for tests
    - [x] `config/worker.php` (`WORKER_BASE_URL`, `INTERNAL_API_SECRET`) + `App\Services\WorkerClient` (thin `Http` facade wrapper — the only class allowed to call the worker)
    - [x] `App\Http\Middleware\VerifyInternalSecret` (constant-time `hash_equals`), aliased `internal.secret`, CSRF-exempted for its one route in `bootstrap/app.php`
    - [x] `InstanceController`: `index`, `create`, `store`, `show`, `status` (JSON, polled every 2.5s), `destroy` (disconnect), `reconnect`. Ownership enforced **both** via a query scoped to `$request->user()` and an explicit `abort_unless` check (defence-in-depth per CLAUDE.md §5); wrong-owner or unknown `instance_id` both 404 (never 403 — doesn't confirm another user's instance exists)
    - [x] `WorkerWebhookController` at `POST /internal/worker/events` (`qr.updated`, `connection.updated`), secret-protected, 404s on an unknown `instance_id`, 422s on an unrecognised event type
    - [x] Views: `instances/index`, `instances/create`, `instances/show` (plain `<img>` for the QR — the worker sends a ready-made PNG data URL, no browser QR library needed; vanilla-JS polling reloads the page once status becomes terminal)
    - [x] Navbar "Instances" link enabled; `DashboardController` now uses real `$user->whatsappSessions()` counts; added session-flash `alert-success`/`alert-danger` to the layout
    - [x] `backend/.env`/`.env.example` gained `WORKER_BASE_URL` + `INTERNAL_API_SECRET` (same secret value as the worker's `.env`, by design)
    - [x] `tests/Feature/InstanceTest.php`, `tests/Feature/WorkerWebhookTest.php`, plus a dashboard-counts test — all use `Http::fake()`, never hit a real worker
  - **Worker (`whatsapp-worker/`):**
    - [x] Installed `baileys` pinned **exactly** `7.0.0-rc14` (no `^`/`~` — npm adds `^` by default, had to hand-edit `package.json`), `qrcode` (turns the raw QR string into a PNG data URL), `@types/qrcode` (dev)
    - [x] `src/whatsapp/sessionManager.ts` — in-memory `Map<instance_id, socket>`; `startSession()`/`stopSession()`; auth files via Baileys' `useMultiFileAuthState()` under `SESSION_STORAGE_PATH`; reports QR + connection changes to Laravel
    - [x] `src/whatsapp/callbacks.ts` — `notifyLaravel()`, posts to `LARAVEL_CALLBACK_URL` with `X-Internal-Secret`, logs (doesn't throw) on failure
    - [x] `src/routes/sessions.ts` — `POST /sessions`, `DELETE /sessions/:instanceId`, both secret-protected. **Replaced** the M4 demo `/internal/ping` route (deleted, along with its test)
    - [x] `config.ts` gained `LARAVEL_CALLBACK_URL` (default `http://127.0.0.1:8000/internal/worker/events`) and `SESSION_STORAGE_PATH` (default `C:\whatsapp-secrets`)
    - [x] Created `C:\whatsapp-secrets\` on disk (outside the repo and outside `htdocs`, per CLAUDE.md §4/§9)
    - [x] `tests/sessions.test.ts` — only covers the auth/validation layer (401s, 400 on bad body, DELETE-of-unknown-id no-ops). **Deliberately does not** exercise a real `POST /sessions` success path in CI — that opens a real Baileys socket and reaches WhatsApp's servers, so it's verified manually instead
  - **Post-live-test bugfix:** the first real QR scan surfaced two bugs, both fixed in `sessionManager.ts`:
    1. Right after a fresh pairing, WhatsApp always closes the connection once with `restartRequired` (515) — expected protocol behaviour, not a real disconnect. The worker was treating it as terminal ("Not connected", needed a manual Reconnect click). Now it reconnects automatically with the saved creds and never bothers Laravel about it.
    2. Phone number was showing the WhatsApp device-id suffix (`919054961320:9`) because `socket.user.phoneNumber` isn't populated, so it fell back to `socket.user.id` unparsed. Fixed to strip both `:` and `@`.
  - Re-verified before installing: `baileys@7.0.0-rc14` was still `latest` on npm (unchanged since M0), `qrcode@1.5.4` current.
  - Committed by the owner as `c985028` (M4) and `05be00c` (M5, includes the bugfix).
- [x] **M6 — API credentials**
  - [x] `api_tokens` migration/table: `whatsapp_session_id` FK cascade-delete, `name`, `token_hash` (unique, SHA-256 — not bcrypt, per CLAUDE.md §6), `token_prefix` (first 8 chars, shown forever in the UI so a token is identifiable without ever re-showing it), `last_used_at`, `revoked_at`
  - [x] `ApiToken` model: `generateFor(WhatsappSession, name)` creates a random 64-char token, returns the **plaintext to the caller only** (never stored/logged); `WhatsappSession::apiTokens()` relation; `ApiTokenFactory` (with a `revoked()` state)
  - [x] `ApiTokenController`: `store` (generate — **gated on `status === 'connected'`**, 422 otherwise, per the owner's explicit choice), `destroy` (revoke — sets `revoked_at`, never hard-deletes). Same ownership pattern as M5: scoped query + explicit `abort_unless`, 404 either way
  - [x] Routes nested under the instance: `POST /instances/{instance}/tokens`, `DELETE /instances/{instance}/tokens/{token}`
  - [x] `instances/show.blade.php` gained an "API credentials" card: existing tokens (name/prefix/created/last-used/Revoke), a one-time plaintext reveal read from `session('new_token')` (gone on the next unrelated page load), a "Generate token" form when connected, a note to connect first otherwise
  - [x] `tests/Feature/ApiTokenTest.php` (9 tests): hash-not-plaintext stored, one-time reveal, connected-gating (422), cross-user (404), cross-instance token/instance mismatch (404)
  - No new packages — `Str::random()` + `hash('sha256', ...)`, both already in Laravel/PHP.
- [x] **M7 — Send text message**
  - **Backend (`backend/`):**
    - [x] `messages` migration/table: `whatsapp_session_id` FK cascade-delete, `direction` (`outgoing` for now), `to_number`, `from_number` (unused until M8), `body`, `status` (`pending`/`sent`/`failed`), `whatsapp_message_id`, `error`
    - [x] `Message` model + `WhatsappSession::messages()` relation + `MessageFactory`
    - [x] `routes/api.php` **created and wired up** (`bootstrap/app.php` had no `api:` routing group before this — added it, prefixes routes with `api/` automatically, matching `CLAUDE.md`'s `/api/v1/...` contract)
    - [x] `AuthenticateApiToken` middleware (alias `api.token`): `Authorization: Bearer` → SHA-256 lookup against `token_hash`, rejects missing/unknown/revoked (401), updates `last_used_at`, attaches the resolved `WhatsappSession` to the request — controllers never trust a caller-supplied user/session id, only what the token itself resolves to
    - [x] `Api\MessageController@send`: validates `instance_id` (uuid) / `to` (7-15 digits) / `message` (≤4096 chars), cross-checks body `instance_id` against the token's own instance (422 on mismatch — defence-in-depth beyond the token alone), 422 if not connected, creates a `pending` message row, calls the worker synchronously, updates to `sent`/`failed`
    - [x] `WorkerClient::sendMessage()` added
    - [x] Rate limiting: `RateLimiter::for('messages', ...)` in `AppServiceProvider` — 30/min, keyed by the bearer token (falls back to IP) — first milestone that can spam a real phone number, so it got one
    - [x] `tests/Feature/MessageTest.php` (9 tests): auth (missing/invalid/revoked), `instance_id` mismatch, not-connected, invalid `to` format, successful send (`Http::fake()`, asserts `sent` + `last_used_at` updated), worker failure (asserts `failed` + `error` stored), rate-limit headers present
  - **Worker (`whatsapp-worker/`):**
    - [x] `sessionManager.ts` gained `sendMessage(instanceId, to, text)` — looks up the live socket, throws `SessionNotActiveError` if the instance isn't actively connected in this worker process (individual chats only — `<to>@s.whatsapp.net`, no group JIDs, per `CLAUDE.md` §0 scope)
    - [x] `src/routes/sessions.ts` gained `POST /sessions/:instanceId/messages` (secret-protected, Zod-validated) — 409 when not active, 502 on any other Baileys failure
    - [x] `tests/sessions.test.ts` — unlike `/sessions`, this route's failure path (no active session) never touches Baileys/network, so it's fully tested here: auth, bad `to`/empty `message` → 400, no active session → 409 (10 tests total in the file)
  - No new packages either side — `Str`/`hash()`/`RateLimiter` (Laravel), Baileys' existing `sendMessage()` (worker).
  - **Verified live:** owner sent a real message via `Invoke-RestMethod`; it arrived (in "Message Yourself", since the test happened to use the connected instance's own number as `to` — a good reminder for next time: pick a genuinely different number to prove third-party delivery); `messages` row confirmed `status = sent` with the real WhatsApp `whatsapp_message_id`.
  - **Rough edge hit during live testing (not a code bug, a dev-workflow gotcha — see "Things to remember"):** the DB can say `connected` while the worker process actually has no live socket, if the worker restarted since the last real connect. First attempt 502'd because of exactly this.
- [x] **M8 — Incoming messages + webhooks**
  - **Worker (`whatsapp-worker/`):**
    - [x] `src/whatsapp/incomingMessage.ts` — new pure function `parseIncomingMessage()`: decides whether a Baileys `messages.upsert` entry is forwarded (skips messages we sent ourselves, groups (`@g.us`), the status/broadcast feed, and anything without plain text) and extracts sender/text/id/timestamp. Pulled out as a pure function (no socket, no network) specifically so this filtering logic — the part most likely to have a bug, e.g. group messages leaking through — can be unit tested directly
    - [x] `sessionManager.ts`: `connect()` now also listens for `messages.upsert`, uses `parseIncomingMessage()`, reports matches via a new `message.received` worker event. Also logs every `messages.upsert` (type + count) and every skip/forward decision — added mid-live-test because success was previously silent, making a real bug indistinguishable from "hasn't arrived yet". Worth keeping permanently, not just a debug throwaway.
    - [x] `callbacks.ts`: `WorkerEvent` union gained the `message.received` variant
    - [x] `tests/incomingMessage.test.ts` (8 tests) — the one piece of M8 that COULD be fully unit tested without a live WhatsApp connection, so it is; everything else stays manual-verification-only, same reasoning as M5/M7
  - **Backend (`backend/`):**
    - [x] `whatsapp_sessions` gained `webhook_url` + `webhook_secret` (nullable; secret auto-generated the first time a URL is set, kept — not regenerated — on later URL changes) — columns on the existing table, not a new one, since it's strictly 1:1 per instance
    - [x] `WorkerWebhookController` gained `message.received` handling: validates the payload, creates a `messages` row (`direction = 'incoming'`, `status = 'received'`), then calls the new `WebhookDispatcher`
    - [x] `App\Services\WebhookDispatcher`: best-effort, synchronous (no queue yet, same MVP stance as M7) POST to the owner's `webhook_url` with the raw JSON body HMAC-SHA256-signed (`X-Webhook-Signature: sha256=...`) using `webhook_secret`; every failure is caught and logged, never thrown — a dead webhook receiver must never break message storage. **Documented, not solved:** this makes a server-side request to a URL the owner typed in (SSRF surface) — acceptable for this MVP's threat model, flagged in the class docblock for later hardening
    - [x] `InstanceController::updateWebhook()` (`POST /instances/{instance}/webhook`) — set/update/clear, same ownership pattern as everything else
    - [x] `instances/show.blade.php` gained: a Webhook card (URL form + visible signing secret + the signature header format spelled out for the owner to implement on their end), and a Recent messages card (last 20, direction badge, number, truncated body, status, relative time) — the UI-scope addition the owner opted into
    - [x] `InstanceController::show()` now also passes `$instance->messages()->latest()->take(20)->get()`
    - [x] Tests: `WorkerWebhookTest.php` (+4: stores incoming messages, dispatches with a verified-correct signature, no dispatch when no webhook configured, dispatch failure doesn't fail the original request), `InstanceWebhookTest.php` (5 tests: set/update-keeps-secret/clear/invalid-URL/cross-user 404), `InstanceTest.php` (+2: show page lists messages / says "No messages yet")
  - No new packages either side.
  - **Verified live** (took several tries — see "Things to remember" for the three separate gotchas hit along the way): two genuine incoming messages from a different phone number, sent as a direct 1-on-1 chat, are stored with `direction = incoming`, `status = received`, confirmed by querying the `messages` table directly rather than trusting the UI alone.

## Post-roadmap hardening (beyond `CLAUDE.md`'s M0-M8 — bugs found through today's own live testing)

- [ ] **Fix: worker forgets "connected" instances on restart** *(code done, not yet live-verified)*
  - [x] New `GET /internal/worker/sessions` (Laravel, secret-protected) — returns `instance_id`s currently `connected`/`connecting`/`qr_pending` (i.e. should have a live socket)
  - [x] Worker's `reconnectAll.ts`: called once at boot (`index.ts`), fetches that list, calls `startSession()` for each — reuses already-saved credentials, no QR needed. Best-effort: if Laravel isn't reachable yet, the worker still starts normally
  - [x] Tests: `InternalSessionsTest.php` (4: secret required, correct status filter, empty case), `tests/reconnectAll.test.ts` (3: fetches + reconnects each id, tolerates Laravel being down, tolerates an error status — all with `startSession` mocked, no real socket)
- [x] **Fix: stale credentials after logout break "Reconnect"** *(code done; found by reading Baileys' source — `logout()` only tells the server to unlink, it never touches the local credential files)*
  - `sessionManager.ts`: new `clearAuthState()` deletes an instance's `SESSION_STORAGE_PATH` folder. Called from two places: the `connection.update` close handler whenever `loggedOut` is the real reason (not just the explicit Disconnect button — an unlink from the phone hits this too), and unconditionally from `stopSession()` (explicit Disconnect), regardless of whether a live socket existed
  - Without this, "Reconnect" after a real logout would silently keep retrying dead credentials forever and never show a fresh QR
- [x] **Fix: a TOCTOU race in `startSession()`** — the "already running?" check and the actual connect were separated by an `await` gap; two near-simultaneous calls for the same instance (e.g. an impatient double-click on Reconnect) could both pass and open two sockets. Added a `startingInstanceIds` guard set synchronously before the gap.
- [x] **Added: "Send a test message" button on the instance page** (the owner's own request, once it was pointed out the API is meant for *their customers'* code to call, not something to test via terminal every time)
  - New `App\Services\MessageSender` — the actual "create a pending row, call the worker, mark sent/failed" logic, extracted so `Api\MessageController` (public API) and this new button share one implementation instead of drifting apart. `Api\MessageController::send()` refactored onto it (all 9 `MessageTest.php` tests still pass unchanged — confirms it's behavior-preserving, not just a rename)
  - `InstanceController::sendTestMessage()` (`POST /instances/{instance}/send-test-message`, `throttle:messages` same as the API), form added to `instances/show.blade.php`, visible only when connected
  - Tests: `InstanceTest.php` (+4: sends and marks `sent`, worker failure marks `failed` + flashes an error, 422 when not connected, 404 for another user's instance)
- All of the above: 86 backend tests passing (was 78), 21 worker tests passing (was 18), Pint clean, `npm run typecheck` clean. **Committed** (`76a0592`). **Not yet confirmed live-verified** — see Next step.

## Post-roadmap features (owner-directed — production-readiness gaps identified when asked "is it finished?")

- [x] **Password reset** *(code done, fully tested, committed `4dc7111`; not yet confirmed live-verified)*
  - Standard Laravel password-broker flow (`Illuminate\Support\Facades\Password`) — the `password_reset_tokens` table has existed since M1's default migrations, unused until now. `User` already supports it via `CanResetPassword` (bundled in the base `Authenticatable` class), no model changes needed.
  - New `Auth\PasswordResetController`: `create`/`store` (request a link — always the same generic "if that email exists…" message and redirect regardless of whether it does, matching login's existing anti-enumeration approach, CLAUDE.md §10 — deliberately overrides Laravel's default per-status message, which would otherwise leak whether an email is registered), `edit`/`update` (the emailed link → set new password)
  - Routes named `password.request`/`password.email`/`password.reset`/`password.update` — `password.reset` specifically is required verbatim, it's hard-coded into Laravel's default `ResetPassword` notification's link-building
  - Views: `auth/forgot-password.blade.php`, `auth/reset-password.blade.php`; "Forgot password?" link added to `auth/login.blade.php`
  - **`MAIL_MAILER=log` in `.env`** (unchanged since M1) — the reset email isn't actually sent anywhere yet, it's written to `storage/logs/laravel.log`. Fine for now; needs a real mail driver (or at least Mailtrap/similar) before this is usable by an actual user, not just in tests.
  - Tests: `PasswordResetTest.php` (7): page renders, guest-only, real-email sends a notification, unknown-email shows the identical message and sends nothing, full reset-then-login-with-new-password round trip (old password stops working, new one works), invalid token rejected without touching the password hash
- [x] **Billing (Cashfree)** *(code done, verified against the real sandbox API, committed `bd3e5d1`; browser checkout confirmed live, see below — automatic webhook confirmation still unverified)*
  - **Pivot from the original Stripe/Cashier proposal**: `PROJECT_BRIEF.md` (which had never actually been read until the owner pointed it out — everything up to now was built from `CLAUDE.md` alone) had real Cashfree sandbox credentials at the bottom of the file. Laravel Cashier doesn't support Cashfree, so this is a hand-built integration against Cashfree's own REST API — plain `Http` calls, no SDK package needed.
  - **Credentials moved** out of `PROJECT_BRIEF.md` (committed, plaintext) into `backend/.env` (git-ignored): `CASHFREE_API_KEY`, `CASHFREE_API_SECRET`, `CASHFREE_ENV=sandbox`. `config/services.php` gained a `cashfree` block.
  - **Pricing** — free tier + 3 paid plans, owner explicitly delegated the exact numbers ("according to you"). Benchmarked against comparable *unofficial* WhatsApp gateways (Wassenger, UltraMsg, Whapi.cloud — not the official Meta Business API, wrong comparison class) and undercut them: Free (₹0, 1 instance, 50 msg/mo), Starter (₹749/mo, 1 instance, 1,000 msg/mo), Growth (₹1,499/mo, 3 instances, 5,000 msg/mo), Business (₹2,999/mo, 10 instances, 50,000 msg/mo). Priced in INR, not the originally-proposed USD — Cashfree's default currency, and consistent with India-focused competitor pricing (AiSensy/Interakt/DoubleTick land in the same ₹1,000-3,000/mo band). All in `config/plans.php`, a plain array — changing a price is a deploy, not a live edit.
  - **`subscriptions` table**: one row per user (`user_id` unique), not a history ledger — updated in place as plan/status change. Auto-created on the `free` plan via a `User::booted()` `created` hook (same pattern `WhatsappSession` already uses for its UUID) — so every path that creates a user (registration, factories, future admin tools) gets billing set up automatically, with no call site able to forget it. Existing pre-migration users (the real dev-DB account) backfilled via a one-off tinker command.
  - **`App\Services\PlanLimiter`**: `canCreateInstance()` / `canSendMessage()` (message limit counts **outgoing only** — incoming messages aren't something the user "spends" quota on). Enforced (422, same pattern as every other policy rejection in this app) in `InstanceController::store()`, `InstanceController::sendTestMessage()`, and `Api\MessageController::send()` — all three checked, none trusts the UI alone.
  - **`App\Services\CashfreeClient`** — verified against Cashfree's real current docs via `WebFetch`/`WebSearch`, not guessed, then corrected twice against **real sandbox API calls** when the docs turned out to be incomplete:
    1. Docs suggested subscriptions could be created with plan details sent inline, no separate Plan object needed. A real sandbox call rejected this — `plan_details.plan_id` turned out to be required even so.
    2. Providing a made-up `plan_id` inline still failed — `"Plan does not exist"` — proving `plan_id` must reference a **pre-created** Plan object at Cashfree after all. Added `ensurePlanExists()` (idempotent — a 409/"already exists" from Cashfree is treated as success), called automatically before every subscribe attempt.
    3. Confirmed end-to-end against the real sandbox: all 3 paid plans created for real, a real `createSubscription()` call returned a genuine `subscription_session_id`.
  - **`BillingController`**: `index` (current plan + usage-this-month progress bars + the 4-plan grid), `subscribe` (validates a phone number — not currently collected anywhere else, so it's asked for right here — creates the Cashfree subscription, sets the local row to `pending`, renders the checkout page directly, since it needs the just-returned session id)
  - **`CashfreeWebhookController`** (`POST /webhooks/cashfree`, CSRF-exempt, no `internal.secret` — authenticated by Cashfree's own signature instead): verified per Cashfree's docs — `base64(HMAC-SHA256(x-webhook-timestamp header + RAW body, client secret))` compared to `x-webhook-signature`, **must** use the raw unparsed body (confirmed in Cashfree's docs, not assumed). Added a 5-minute replay-window check as defence-in-depth (not something Cashfree's docs specify, a reasonable default). **The exact event-payload envelope (which JSON keys hold the subscription id/status) is not yet confirmed against a real webhook delivery** — logged in full so it can be adjusted once we see one; never 500s on an unrecognised shape.
  - Views: `billing/index.blade.php`, `billing/checkout.blade.php` (loads `https://sdk.cashfree.com/js/v3/cashfree.js`, calls `cashfree.subscriptionsCheckout({ subsSessionId, redirectTarget: '_self' })`), "Billing" navbar link.
  - **Post-live-test fix**: completing a real checkout surfaced a `MethodNotAllowedHttpException` — Cashfree sends the browser back to `return_url` via a **POST**, not a GET redirect, which nothing in the app anticipated. Added `GET|POST /billing/return` (`BillingController::return()`, just bounces to `/billing`), deliberately outside the `auth` group and CSRF-exempt — Cashfree's own POST carries neither our session nor a CSRF token, and this route is UX-only (the webhook, not this route, is the source of truth for whether payment succeeded). `CashfreeClient::createSubscription()`'s `returnUrl` now points here instead of straight at `billing.index`.
  - **Not implemented (known gap, not started)**: self-serve downgrade/cancellation — only the upgrade/subscribe direction exists.
  - **Webhook confirmation deliberately not verified live, and that's OK for now**: checked the database and log directly after a real checkout attempt — `subscriptions.status` stayed `pending`, and the log shows zero real delivery attempts from Cashfree (every `Cashfree webhook received` line present is from the automated test suite, not a real one). Root cause understood, not a bug: `php artisan serve` on `127.0.0.1:8000` isn't reachable from the public internet, so Cashfree's servers have no way to deliver a webhook to it — the same limitation every webhook-based integration hits in local dev (Stripe, GitHub, etc.), normally solved with a tunnel (ngrok or similar) pointed at a real dashboard-registered webhook URL. Owner chose to defer this rather than set up a tunnel right now — genuinely verifying it needs either that, or an actual deployment with a public URL. Subscription creation and checkout themselves ARE confirmed working (real plans + a real subscription session, per the sandbox smoke test above); only the automatic "checkout → webhook → active" confirmation is unverified.
  - Tests: `BillingTest.php` (11), `CashfreeWebhookTest.php` (6, including a genuinely-computed valid signature and a stale-timestamp rejection), plus instance-limit and message-limit enforcement tests added to `InstanceTest.php`/`MessageTest.php`. All use `Http::fake()` — the real-sandbox verification above was done separately, by hand, via `tinker`, specifically so the test suite stays fast/offline/deterministic like everything else in this project.
  - **Real local-environment bug found and fixed along the way**: PHP on this XAMPP install had `curl.cainfo`/`openssl.cafile` both unset and no CA bundle file anywhere — every outbound HTTPS call from PHP failed with `SSL certificate problem: self-signed certificate in certificate chain`, confirmed both via a direct sandbox call from Claude and via the owner's own real browser attempt (same error, same log). Fixed (with the owner's explicit approval, per `CLAUDE.md`'s "don't touch `php.ini` without asking"): downloaded the standard Mozilla CA bundle to `C:\xampp\php\extras\ssl\cacert.pem`, set `curl.cainfo`/`openssl.cafile` to point at it. `php.ini` backed up first as `php.ini.pre-cacert-fix.bak`, matching this project's established config-change convention. **Owner needs to restart `php artisan serve` to pick this up.**

## Queues + webhook retries (owner-directed, scale/reliability concern)

- [x] **Infrastructure**: `QUEUE_CONNECTION=database` — already configured since M1 (default Laravel), `jobs`/`failed_jobs` tables already migrated. No Redis, no new install, matching `CLAUDE.md`'s "don't introduce Redis until a milestone actually needs it." **Owner must run `php artisan queue:work` as an additional long-running process** (same idea as `php artisan serve` and the worker's `npm run dev`) — queued jobs sit in the `jobs` table doing nothing until a worker process picks them up. Not started automatically by anything.
- [x] **`App\Jobs\DeliverWebhook`** — the M8 incoming-message → customer-webhook delivery, now queued instead of synchronous-best-effort. `$tries = 5`, `backoff()` spread over 10s/1m/5m/15m/30m (not hammering a receiver that's only briefly down). `failed()` logs a warning once retries are exhausted. The actual HTTP call + HMAC signing logic moved here from `WebhookDispatcher`, which is now just a 5-line "is there anything to deliver to?" check that dispatches the job — kept as a class specifically so `WorkerWebhookController` didn't need to change at all.
- [x] **Deliberately NOT queued: message sending** (both the public API and the dashboard's "Send a test message" button). Queuing it would change the public API's response contract — today a caller gets `{success, message_id}` immediately; queued, that would have to become "accepted, poll or wait for a webhook instead." That's a real breaking change for real integrators, not just an internal reliability tweak, so it wasn't bundled into this pass without a separate explicit decision. Flagged to the owner; not objected to.
- [x] **Deliberately NOT queued: starting a WhatsApp session** (worker call from creating/reconnecting an instance) — considered, but rejected: the current synchronous path gives an immediate "worker unreachable" error message if the worker's down; queuing it would silently lose that feedback (the page would just sit on "waiting for QR" forever with no explanation). Not what was actually asked for (the stated concern was message bursts + webhook reliability specifically), so left alone.
- Tests: `WorkerWebhookTest.php`'s webhook-dispatch tests rewritten from `Http::fake()`/`Http::assertSent()` to `Queue::fake()`/`Queue::assertPushed(DeliverWebhook::class, ...)`, since the HTTP call no longer happens synchronously during the request. New `DeliverWebhookTest.php` (6 tests) covers the job's own behaviour directly: correct signature, no-op when the webhook was cleared before the job ran, no-op when the instance no longer exists, throws (so the queue retries) on a delivery failure, `failed()` logs correctly, `backoff()`/`tries` are what's documented above.

## Account settings (owner-directed; 2FA/audit-logs/admin-panel explicitly deferred as separate future work)

- [x] **Change password** — `AccountController::updatePassword()`, requires the current password (Laravel's built-in `current_password` validation rule), same `Password::min(8)` policy as registration/reset.
- [x] **Delete account** — `AccountController::destroy()`, requires the current password (own named error bag, `deleteAccount` — the page has two forms sharing a `current_password` field, so without a named bag a validation error from either form would incorrectly show on both). Best-effort logs out every one of the user's WhatsApp instances at the worker first (so devices aren't left dangling); the account row itself cascades to `whatsapp_sessions`/`api_tokens`/`messages`/`subscription` via existing FK `cascadeOnDelete` constraints regardless of whether that worker cleanup succeeds. **Blocks deletion while an active paid subscription exists** (clear error message pointing at contacting support) — self-serve Cashfree cancellation doesn't exist yet (known gap, noted under Billing above), and silently deleting the account would leave Cashfree trying to bill a card for good.
- [x] Clicking your own name in the navbar now opens `/account` (no separate nav link added — kept the nav uncluttered).
- Tests: `AccountTest.php` (8): page renders/guest-blocked, password change works + requires the right current password, account deletion actually removes the user and cascades to their instances/tokens/messages, requires the right password (independently of the password-change form, per the named bag), blocked with an active paid subscription, and proceeds even if the worker is unreachable (best-effort, not a hard dependency).

## API Docs page (owner-directed, filled the M3-era "API Docs — soon" navbar placeholder)

- [x] `App\Http\Controllers\ApiDocsController` (single-action) + `GET /docs` route (named `docs.index`, inside the `auth` group)
- [x] `resources/views/docs/index.blade.php` — static Blade page: getting-started steps (create instance → connect → generate token), auth (`Authorization: Bearer`), the one endpoint's request fields / response table (pulled directly from `Api\MessageController`'s actual status codes, not re-invented), a 30/min rate-limit note, and code samples in curl / JavaScript (`fetch`) / PHP switched via Bootstrap nav-tabs (`data-bs-toggle="tab"` — no new JS package, `import 'bootstrap'` in `app.js` already includes the Tab plugin)
- [x] Navbar's disabled "API Docs — soon" placeholder ([app.blade.php](backend/resources/views/layouts/app.blade.php)) replaced with a real active link
- [x] `tests/Feature/ApiDocsTest.php` (2 tests: guest redirected, logged-in user sees the endpoint documented)
- [x] Live-smoke-tested: `npm run build`, then a real `php artisan serve` on port 8001 with a throwaway `@example.invalid` user (created and deleted via `tinker`, per this file's own "Things to remember" convention) — confirmed guest → 302, logged-in → 200 with the nav link, tabs, and endpoint table all present in the actual rendered HTML.
- No new packages.

## Design overhaul, phase 1: shared layout + Dashboard (owner-directed — "I don't like the design right now", full redesign, page-by-page starting with Dashboard)

- **Decisions made via `AskUserQuestion`** (owner's picks, not assumed): top navbar (enhanced, not sidebar), WhatsApp-inspired deep teal-green brand accent (not neutral blue/indigo), and approval to add `bootstrap-icons` as a new package.
- [x] **New package**: `bootstrap-icons` (devDependency, `backend/package.json`) — official Bootstrap 5 companion icon set, pure CSS/SVG font, no JS, MIT license. Imported once in `resources/css/app.css`.
- [x] **Brand palette** (`resources/css/app.css`): `--wa-primary: #0b8457` / `--wa-primary-dark: #086b46` / `--wa-primary-light: #e7f7ef` / `--wa-dark: #10231c`. **Important, non-obvious:** Bootstrap's *precompiled* `bootstrap.min.css` hardcodes most component colors (e.g. `.btn-primary`'s background) as literal hex values rather than reading the top-level `--bs-primary` variable — so a single `--bs-primary` override does nothing for buttons in this setup (we import the prebuilt dist CSS, not the Sass source). Retinted instead by overriding each component's own local custom properties (`--bs-btn-bg`, `--bs-btn-hover-bg`, etc. — Bootstrap 5.3's own documented pattern for this exact situation), plus direct rules for `.text-primary`/`.bg-primary`/`.badge.text-bg-primary`/links/navbar/footer. **If a future color tweak "does nothing," check whether the class being changed uses local `--bs-*` custom properties or a hardcoded hex — that's almost certainly why.**
- [x] **`layouts/app.blade.php` redesigned**: brand mark (chat-bubble icon + app name) in the navbar, icons added to every nav link, the plain-text account name + separate logout button replaced with a single Bootstrap dropdown (avatar icon, Account/Log out inside it — `dropdown-toggle`/`data-bs-toggle="dropdown"`, no new JS package needed since `import 'bootstrap'` in `app.js` already includes the Dropdown+Popper bundle), and a new `<footer class="site-footer">` (brand, quick links to API Docs/Billing when logged in, copyright) pinned to the bottom via `d-flex flex-column min-vh-100` on `<body>` + `flex-grow-1` on `<main>`.
- [x] **`dashboard.blade.php` redesigned**: hero banner (soft-mint background, greeting + a "Create instance" CTA), the three summary cards gained icon badges, and "Getting started" now shows a filled vs. outline check-circle per step **driven by real data already available on the controller** (`$instanceCount`/`$connectedCount` — step 1/2 "done" state), not just static text. No new controller/query logic added — reused what `DashboardController` already passes to the view, per `CLAUDE.md`'s "no speculative architecture."
- [x] All literal strings the test suite depends on were preserved verbatim while restyling around them (`tests/Feature/DashboardTest.php`'s `assertSee('Welcome, Jane Doe')`, `'Instances'`, `'No instances yet'`, `'1 of 2 connected'`, `'Dashboard'`, `'API Docs'`, `'Log out'`, etc.) — confirmed by re-running the full suite, not by inspection alone.
- [x] `npm run build` succeeded; `php artisan test`: 128/128 still passing; `vendor/bin/pint --test`: clean.
- [x] Live-smoke-tested exactly like the API Docs page: real `php artisan serve` on port 8001, throwaway `@example.invalid` user via `tinker` (created, tested, deleted), confirmed in the actual served HTML/CSS that the icon font's `@font-face` URLs resolve through Vite's build and the font file itself returns `200`, not just that the page renders.
- **Scope, per the owner's own phasing ("first dashboard, then all pages next")**: this pass covers the shared layout (navbar/footer, used everywhere) and the Dashboard page's own content only. Instances/Billing/Account/API Docs/Auth pages still use the old plain-Bootstrap card styling for their page-specific content — next up, one at a time.
- **Superseded by phase 2 below within the same session** — the owner saw a sidebar-style mockup they liked better and asked to switch. The top-navbar shell described above no longer exists in the code; kept here only as a record of the first pass and the "hardcoded hex vs. `--bs-*` custom property" lesson, which still applies.

## Design overhaul, phase 2: sidebar layout (owner shared a mockup, explicitly chose sidebar over the top navbar from phase 1)

- **Decisions made via `AskUserQuestion`**, in reaction to a mockup the owner shared: switch to a real, permanent sidebar (not sidebar-for-Dashboard-only — applies to the shared layout, every page) — and explicitly **omit** the mockup's search bar and notification bell, since neither corresponds to a real feature in the app (no search index, no notification system) and a dead/non-functional control was judged worse than not having one, per `CLAUDE.md`'s stance against speculative/placeholder UI.
- [x] **`layouts/app.blade.php` rebuilt around a sidebar shell**: `@auth`/`@else` now branch into two entirely different chromes —
  - **Logged in**: `.sidebar-shell` (dark green, `260px`, brand mark + two-line "WhatsApp / Gateway" wordmark, `nav-pills` for Dashboard/Instances/Billing/API Docs with a light-mint "active" pill, a footer block with tagline + copyright) alongside a content column with its own slim white top bar (mobile hamburger + an avatar-initial dropdown for Account/Log out, replacing the earlier person-icon dropdown).
  - **Guest** (login/register/forgot-password/reset-password — all still `@extends('layouts.app')`): unchanged simple dark top navbar with Log in/Register, no sidebar at all — confirmed by asserting zero sidebar markup appears in a guest-rendered page, not just by reading the `@else` branch.
  - **Responsive, no custom JS**: used Bootstrap 5.3's built-in `.offcanvas-md` — a single element that's a normal always-visible flex column at `md`+ and a real slide-in offcanvas (with its own open/close button) below it. This is Bootstrap's own documented pattern for exactly this "sidebar that collapses on mobile" case; no new package, no hand-rolled toggle script.
  - The earlier phase-1 `<footer>` was removed — its brand/copyright role is now folded into the sidebar's own footer block, so there's one branding spot instead of two competing ones.
- [x] **`dashboard.blade.php` rebuilt to match the mockup's content, not just the phase-1 restyle**: hero banner gained a hand-crafted CSS+icon "illustration" (two soft blob circles behind a white rounded "phone" card with a WhatsApp-badge overlay — `bi-phone` + `bi-whatsapp`, no image asset or illustration library, since the icon font was already approved), the three stat cards gained a left-border accent color each (green/blue/purple — two new palette entries, `--wa-info`/`--wa-purple`, added specifically for this), and "Getting started" switched from check/circle icons to numbered circular badges (1/2/3, one accent color each) with a small description line per step, matching the mockup exactly rather than phase 1's own take.
- [x] **Test fix required, not just a color/markup change**: the hero now renders "Welcome," and the user's name in two separate `<h1>`/`<div>` elements (matching the mockup's two-line greeting) instead of one combined string, which broke `DashboardTest.php`'s `assertSee('Welcome, Jane Doe')` (that exact substring no longer exists in the DOM). Fixed by splitting the assertion into `assertSee('Welcome,')` + `assertSee('Jane Doe')` in both affected tests — an intentional design change, not a regression, so the test was updated to match the new markup rather than forcing the old string back.
- [x] `npm run build` succeeded; `php artisan test`: 128/128 passing (474 assertions, up from 472 — the two updated assertions still count individually); `vendor/bin/pint --test`: clean.
- [x] Live-smoke-tested: real `php artisan serve` on port 8001, throwaway `@example.invalid` user via `tinker` — confirmed guest pages contain zero sidebar markup, the authenticated dashboard's actual served HTML contains every new class (`sidebar-shell`, `offcanvas-md`, `hero-illustration`, `hero-phone-badge`, `avatar-badge`, all three `stat-card-*` and `step-number-*` variants), and the built CSS file itself (not just the HTML referencing it) contains the new palette variables and component rules — not eyeballed, grepped directly out of the response bodies.
- **Scope unchanged from phase 1's own note**: still just the shared layout + Dashboard. Instances/Billing/Account/API Docs/Auth pages now sit inside the new sidebar shell automatically (since it's the shared layout), but their own page content hasn't been restyled yet — that's still the next milestone, one page at a time, owner's choice on order.

### Follow-up fix (owner caught it live): sidebar was invisible at desktop width, active nav item was Bootstrap-default blue instead of brand green

The owner's own screenshot of a real logged-in page (`/instances`) showed the sidebar background essentially gone (near-white, with the 75%-white nav text unreadable against it) and the active nav pill rendering plain Bootstrap blue, not the brand green. Root-caused by reading Bootstrap's actual *compiled* `bootstrap.min.css` directly (`grep`, not guessing) rather than assuming our overrides should have worked:

1. **`.offcanvas-md`'s own desktop reset uses `background-color: transparent !important`** on both the offcanvas element and its `.offcanvas-body`, specifically so it "de-offcanvas-ifies" into plain static content at `md`+. A normal `background-color` declaration — ours — has zero chance against `!important` no matter its specificity or where it sits in the file.
   **Fix**: `.sidebar-shell, .sidebar-shell .offcanvas-body { background-color: var(--wa-dark) !important; }` — matching `!important` with `!important`, the only way to win that fight.
2. **`.nav-pills` declares its own `--bs-nav-pills-link-active-bg: #0d6efd` (Bootstrap's default blue)**, and `.nav-pills .nav-link.active` reads that variable. Our `.sidebar-link.active` rule (2 classes) was always going to lose to `.nav-pills .nav-link.active` (3 classes) on plain CSS specificity, regardless of `!important` or source order.
   **Fix**: override the *variable* at a more specific scope instead of fighting the component rule directly — `.sidebar-shell .nav-pills { --bs-nav-pills-link-active-bg: var(--wa-primary-light); --bs-nav-pills-link-active-color: var(--wa-primary-dark); }`. Bootstrap's own higher-specificity rule still "wins" the selector match, but it now reads our corrected variable value, so the visible result is correct. `.sidebar-link.active`'s own color/background declarations were removed as dead code once this was in place (documented with a one-line comment pointing at why).
- **General lesson, now worth remembering for every future page restyle**: this project imports Bootstrap's *precompiled* dist CSS, not the Sass source, so two different failure modes show up depending on the component: some (buttons) hardcode colors as literal hex and ignore `--bs-primary` entirely (documented in phase 1); others (nav-pills, badges, links) *do* read a `--bs-*` variable, but declare their own default value for it on the component's own selector, which then out-specifies a naive override. When a color override "does nothing" or "half-works," `grep` the actual rule out of `node_modules/bootstrap/dist/css/bootstrap.min.css` before guessing why — both bugs here were found this way, not by trial and error.
- Both fixes verified the same rigorous way as everything else in this file: rebuilt (`npm run build`), grepped the *built* CSS output (not just the source) to confirm both rules compiled with the right selectors/`!important`, re-ran the full test suite (128/128) and Pint (clean), then a fresh live smoke test via a new throwaway account.

## Design overhaul, phase 3: Instances (list + create + detail) — owner's chosen order after Dashboard

- [x] **`instances/index.blade.php`**: header row with subtitle + icon "New instance" button; empty state gained a centered icon badge (matches Dashboard's empty-state pattern); each row's name gained a small `bi-hdd-stack` icon badge; status changed from a plain `badge text-bg-*` to a rounded-pill badge with a small colored dot (`bi-circle-fill`) in front of the label — same semantic colors as before (`success`/`warning`/`secondary`), just restyled, not reassigned.
- [x] **`instances/create.blade.php`**: form card gained an icon-badge header (`bi-hdd-stack`, matches the established "Getting started"/dashboard header pattern) and the submit button gained a `bi-qr-code` icon; form fields/validation/behavior untouched.
- [x] **`instances/show.blade.php`** (the big one): every section now uses the same icon-badge-header pattern as the rest of the app, each with its own accent color so the sections are visually distinct at a glance — status hero (green check-circle when connected / muted x-circle when disconnected / blue qr-code icon while waiting for a scan, replacing the old plain `text-success`/`display-6` text), Send test message (blue, `bi-send`), API credentials (purple, `bi-key`), Webhook (blue, `bi-link-45deg`), Recent messages (green, `bi-chat-left-text`). Copy buttons for `instance_id`/new token switched from text "Copy" to a `bi-clipboard` icon (no test depended on the word "Copy").
  - **Nothing behavioral changed**: the polling script's three DOM hooks (`#instance-status[data-instance-status]`, `#qr-holder`, `#qr-image`) are untouched — restyling only changed the classes around them, the JS diff is effectively zero. All form actions/field names/CSRF/method-spoofing untouched.
- [x] All literal strings the test suite depends on preserved verbatim (`$instance->name`, `'No instances yet'`, the QR `data:image/png;base64,...` src, `'Connected'`, `$instance->phone_number`, `$instance->instance_id`, message body/number text, `'No messages yet'`, `'Reconnect'`, token prefix) — confirmed by re-running `InstanceTest`/`ApiTokenTest`/`InstanceWebhookTest` (35 tests), not just by inspection.
- [x] `npm run build` succeeded; `php artisan test`: 128/128 passing, no assertion changes needed this time (unlike Dashboard's phase 2); `vendor/bin/pint --test`: clean.
- [x] Live-smoke-tested more thoroughly than previous pages, since this page has multiple real states to check: a throwaway account with **three** instances seeded directly via factories — one `connected` (with a real API token and a real message row), one `qr_pending` (with a fake QR data URL) — confirmed in the actual served HTML that every state-specific icon/section renders correctly (`bi-check-circle-fill` on the connected page, `bi-qr-code`/the QR image on the pending page, the token name/message body/number all present), not just that the pages return `200`. Cleaned up (tokens/messages/instances/user all deleted) afterward.

## Design overhaul, phase 4: Billing — owner asked to "make it stronger, add more text if needed"

- [x] **`config/plans.php`** gained two new content-only keys per plan (no functional/pricing change): `description` (a one-line tagline — e.g. Growth: "For teams running multiple numbers and steady campaigns") and `popular` (`true` only on `growth`, drives a "Most popular" ribbon). `CashfreeClient` only reads the specific keys it needs (`cashfree_plan_id`/`name`/`price`), confirmed by reading it first — adding unrelated keys to the same array is safe.
- [x] **Copy additions were kept honest, not aspirational** — deliberately did **not** add feature bullets like "priority support" or "cancel anytime" that aren't actually true yet (no support system exists; self-serve cancellation is a known, documented gap — see the Billing section above). The four bullets added to every plan card (instance count, message count, "Full REST API access", "Webhook delivery") are all real, currently-shipping capabilities, true for every tier — repeating them on each card is a legitimate pricing-page pattern, not a fabrication.
- [x] **`billing/index.blade.php` redesigned**: page subtitle, current-plan card gained the same icon-badge header pattern as every other page (purple, `bi-credit-card`), usage progress bars now turn red past 90% of the plan limit (a real, computed threshold — not decorative) and gained icon labels; each plan card gained a tier icon (`bi-gift`/`bi-lightning-charge`/`bi-graph-up-arrow`/`bi-building`), its new tagline, a checkmark-icon feature list instead of a plain `<ul>`, and the Growth card gets a "Most popular" pill ribbon plus a highlighted border.
- [x] **`billing/checkout.blade.php`**: icon badge + minor copy polish only — the Cashfree SDK integration, auto-start-checkout behavior, and error handling are byte-for-byte unchanged.
- [x] All literal strings the test suite depends on preserved (`BillingTest.php`'s `assertSee('Free'/'Starter'/'Growth'/'Business')`, the checkout page's `subs_session_abc123` session id) — confirmed by re-running the suite, not by inspection.
- [x] `npm run build` succeeded; `php artisan test`: 128/128 passing, no assertion changes needed; `vendor/bin/pint --test`: clean.
- [x] Live-smoke-tested: a throwaway account set directly to the `growth` (popular) plan with `active` status via `tinker` — confirmed in the actual served HTML that the new taglines, all four tier icons, the "Most popular" ribbon, and the disabled "Current plan" button (specifically on the Growth card, not just present somewhere on the page) all render correctly, and that the usage progress bars correctly have *no* danger-red class at low usage (the threshold logic isn't just present, it's evaluating correctly). Checkout page's real-API path wasn't re-verified live this pass (it's icon/copy-only, and the automated suite's `Http::fake()` coverage plus the earlier session's real-sandbox verification already cover it) — cleaned up the throwaway user afterward.
- **Next**: owner picks the next page — Account, API Docs, or Auth pages.

## Bug fix (owner hit it live): plan-limit rejections crashed with a raw Laravel error page instead of a friendly message

Trying to create a 2nd instance on the free plan (limit 1) threw an uncaught `HttpException` and showed Laravel's raw debug stack trace instead of redirecting back with a message — the owner saw this directly while testing.

- **Root cause**: `InstanceController::store()` and `InstanceController::sendTestMessage()` used `abort_unless($condition, 422, "message")` for the plan-limit (and, in `sendTestMessage`, the not-connected) checks. `abort_unless` is correct for the JSON API (`Api\MessageController` already does this properly, unaffected), but on a plain web route it throws an `HttpException` that Laravel renders as its own error page — not a redirect back to the form, unlike every *other* rejection in this same controller (e.g. "worker unreachable" a few lines below, which already used `redirect()->with('error', ...)`). These two checks just never got updated to match that established pattern.
- **Fix**: both checks now `return redirect()->route(...)->with('error', '...')` instead of aborting — `store()` redirects back to `instances.create` (so the "One too many" attempt lands back on the form with the message), `sendTestMessage()` redirects to `instances.show` for both its checks (not-connected, and message-limit), matching the failed-send branch already in the same method.
- **Test coverage gap found and closed**: `sendTestMessage()`'s message-limit branch had *no test at all* before this — only its "not connected" sibling check was tested. Added `test_cannot_send_a_test_message_past_the_plans_message_limit` (seeds 50 real outgoing messages, the free plan's exact limit, then confirms the 51st is rejected with a redirect+flash, not a 51st row created). The two existing tests that asserted the old raw `assertStatus(422)` behavior (`test_cannot_create_an_instance_past_the_plans_limit`, `test_cannot_send_a_test_message_when_not_connected`) were updated to assert `assertRedirect(...)->assertSessionHas('error')` instead — the crash was the bug, so the test that expected it had to change, not the fix.
- [x] `php artisan test`: 129/129 passing (128 + 1 new); `vendor/bin/pint --test`: clean.
- [x] Live-reproduced the exact scenario: a throwaway free-plan account already at its 1-instance limit, a real POST to `/instances` through the actual HTTP layer (not just the test client) — confirmed a `302` to `/instances/create`, the red alert box with the friendly message in the actually-served HTML, and (via `tinker`) that the instance count stayed at exactly `1`, not `2` — the rejection still works, only the presentation changed. Cleaned up afterward.

## Design overhaul, phase 5: Account, API Docs, Auth pages — completes the page-by-page restyle

Owner said "any order," so all three went in one pass since each is small and low-risk (no new states/logic to design around, unlike Instances).

- [x] **`account/edit.blade.php`**: account-details card now leads with an avatar-initial badge (same `.avatar-badge` component the top bar dropdown already uses, reused rather than inventing a second avatar style); Change password gained a blue `bi-shield-lock` icon-badge header; Delete account gained a red/danger icon-badge header (`bi-exclamation-triangle`, `bg-danger-subtle`) — the card's existing `border-danger` treatment was kept, just given a matching header instead of a plain `card-header`. Both submit buttons gained icons. No field names, routes, or the `deleteAccount` named error bag touched.
- [x] **`docs/index.blade.php`**: all four sections (Getting started, Authentication, Endpoint reference, Code samples) gained the same icon-badge-header pattern as every other page; "Getting started"'s numbered list switched from a plain `<ol>` to the same `step-number-1/2/3` circular badges introduced on the Dashboard, reusing that component rather than inventing a new one. All endpoint documentation content (request fields, response codes/bodies, rate limit, code samples in curl/JS/PHP) is unchanged — this was styling only, since the reference material itself was already accurate.
- [x] **Auth pages** (login, register, forgot-password, reset-password — all render inside the guest chrome, no sidebar): each gained a centered brand icon above the card (chat-bubble mark on login/register, a key icon on the two password-reset pages, matching their purpose), and each submit button gained an icon. Form fields, validation, `old()` repopulation, and route names untouched.
- [x] All literal strings the test suite depends on preserved (`AccountTest`'s user name/email, `AuthTest`'s `'Create your account'`/`'Log in'`/`'Jane Doe'`) — confirmed by re-running the suite, not by inspection.
- [x] `npm run build` succeeded; `php artisan test`: 129/129 passing, zero assertion changes needed across all three page groups; `vendor/bin/pint --test`: clean.
- [x] Live-smoke-tested every page in this batch in one pass: a throwaway account (created, then deleted at the end) hit `/login`, `/register`, `/forgot-password` as a guest; a real password-reset token generated via `Password::createToken()` (not a fake string) to load `/reset-password/{token}` and confirm it actually accepts a real token, not just any URL shape; then logged in for real and hit `/account` and `/docs` — confirmed every new icon/component class from each bullet above is actually present in the served HTML, not just assumed from the source.
- **This closes the full page-by-page design overhaul** started after Dashboard: Dashboard → Instances → Billing → Account/API Docs/Auth, all now sharing one consistent visual language (icon-badge section headers, the WhatsApp-green palette, the `step-number-*`/`avatar-badge`/`bg-wa-light` components reused across pages rather than redefined per page). Nothing left unstyled except pages that don't exist yet.

## Feature: Remember me on login (owner-requested)

- [x] **`LoginController::store()`**: one-line change — `Auth::attempt($credentials, $request->boolean('remember'))` instead of `Auth::attempt($credentials)`. Pure Laravel built-in: the `remember_token` column has existed on `users` since M1's default migrations and was simply unused until now; no new migration, no new package.
- [x] **`auth/login.blade.php`**: added a "Remember me" checkbox (`name="remember"`), placed on the same row as "Forgot password?" (previously its own line under the password field) rather than adding vertical space with a new row.
- [x] Tests: `test_remember_me_checkbox_issues_a_persistent_remember_token` and `test_login_without_remember_me_does_not_issue_a_remember_token` — both assert directly on `remember_token` in the database (starts `null`, checked → non-null / unchecked → stays `null`), the actual mechanism, not just "login still redirects."
- [x] `npm run build` succeeded; `php artisan test`: 131/131 passing (129 + 2 new); `vendor/bin/pint --test`: clean.
- [x] Live-verified beyond what the test suite can show: a real POST to `/login` with `remember=on` through the actual HTTP layer, inspecting the raw `Set-Cookie` response headers directly (`curl -v`) — confirmed a real `remember_web_<hash>` cookie was issued with `Max-Age=34560000` (~400 days, Laravel's default remember duration), not just that *a* cookie was set. Also confirmed via `tinker` that `remember_token` was actually persisted to the `users` row (60 characters, matches Laravel's expected format). Cleaned up the throwaway user afterward.

## Pre-launch prep, part 1 of 3: Terms of Service page (owner's plan: ToS → billing cancellation → admin panel)

- [x] **`App\Http\Controllers\TermsController`** (single-action) + `GET /terms`, deliberately **public** — outside both the `guest` and `auth` middleware groups in `routes/web.php`, since a ToS page needs to be readable before registering and afterward. It renders inside whichever chrome the visitor already gets (sidebar if logged in, simple navbar if not) — no special-casing needed, since `layouts/app.blade.php` already branches on auth state for every page.
- [x] **`resources/views/terms/index.blade.php`** — actual drafted content, not a placeholder. Covers: what the Service is (with an explicit, prominent disclosure that it's an **unofficial** WhatsApp Web-style integration, not affiliated with or endorsed by WhatsApp/Meta, and that WhatsApp may restrict/ban a connected number for reasons outside the operator's control — this is the single most legally important paragraph on the page, given `CLAUDE.md`'s own stance on the unofficial-integration risk), account responsibility, acceptable use (no spam, no bypassing WhatsApp's own enforcement, no touching other users' data), subscriptions/billing/cancellation (Cashfree, INR, worded to already describe self-serve cancellation even though that's not built yet — see the caveat below), data handling, service-availability/no-SLA disclaimer, termination, limitation of liability, governing law (India), and a contact email.
- [x] **Linked from two real places**: the register page (a line under the submit button — "By creating an account, you agree to our Terms of Service"), and the sidebar footer for logged-in users (next to the copyright line). Not linked from the guest login/forgot-password pages, since register is the one place actual agreement happens.
- **Explicitly flagged to the owner, not silently assumed**: this is AI-drafted first-draft legal content, not a substitute for review by an actual lawyer before relying on it for real signups — especially the billing, liability, and governing-law sections. Placeholders used and worth confirming: the operating entity name ("BriskBrain Technologies", taken from the configured git commit author — not verified as the actual registered legal entity) and the contact email (the owner's own personal Gmail from this session's context — worth replacing with a dedicated support address once a domain exists).
- **Deliberate wording choice, worth remembering**: section 4 (billing) already describes self-serve cancellation ("cancel your subscription at any time from your account's Billing page, or by contacting us") even though that feature doesn't exist yet as of this commit — it's part 2 of the owner's stated 3-part plan for this session (ToS → cancellation → admin panel) and is expected to exist by the time this page is ever seen by a real signup. If cancellation ends up NOT shipping before launch, or ships differently than "from the Billing page," this section needs a matching update — don't let the two drift apart.
- [x] `tests/Feature/TermsTest.php` (3 tests): guest can view it, logged-in user can view it, and the register page actually contains a link to it (not just that the content exists in isolation).
- [x] `npm run build` succeeded; `php artisan test`: 134/134 passing (131 + 3 new); `vendor/bin/pint --test`: clean.
- [x] Live-smoke-tested: confirmed in real served HTML that a guest gets `200` with the "not affiliated with, endorsed by" disclosure text and the operator/contact details actually present (not just that the route resolves), that the register page's rendered HTML contains a real `href` to `/terms` (not just assumed from the Blade source), and that the sidebar footer link renders the same way for a logged-in throwaway account. Cleaned up afterward.
- **Next**: part 2, billing cancellation.

## Pre-launch prep, part 2 of 3: billing self-serve cancellation (closes a known gap flagged since the original Billing build)

- [x] **`CashfreeClient::cancelSubscription()`** — new method, `POST /subscriptions/{id}/manage` with `{"action": "CANCEL"}` (Cashfree's "Manage Subscription" API). **Verified against a real sandbox call, not just Cashfree's docs** — this project's own hard-learned rule (`ensurePlanExists()`/`createSubscription()` both needed correction beyond what the docs said). This time the docs were actually accurate: created a real throwaway sandbox subscription via `tinker`, cancelled it with the new method, got back `"subscription_status": "CANCELLED"` with no undocumented required fields.
- [x] **`BillingController::cancel()`** (`POST /billing/cancel`) — calls Cashfree to cancel, then **immediately** reverts the local `subscriptions` row to `plan: 'free'`, `status: 'active'`, `cashfree_subscription_id: null`, `current_period_end: null` (the free-plan convention already documented in the migration itself). Rejects with a friendly redirect+flash (not an abort — learned from the earlier plan-limit bug) if the user has no paid subscription to cancel. On a Cashfree failure, changes nothing and flashes an error — same defensive pattern as `subscribe()`.
- **Deliberate design choice, worth remembering**: cancellation is immediate, not "access until the end of the billing period" (the more common SaaS convention). Reason: `current_period_end` isn't reliably populated by anything today — the webhook that would confirm real payment/period data has never been verified live (needs a public URL, still an open gap). Building a grace-period mechanism against a date nothing currently sets would be exactly the kind of speculative complexity `CLAUDE.md` warns against. The Terms of Service page (part 1) was worded to stay compatible with this — it promises no refund for the current period, not continued access, so there's no contradiction between the two. If `current_period_end` ever becomes reliably populated (e.g. once the Cashfree webhook is verified live post-launch), revisit whether cancellation should become end-of-period instead.
- [x] **`billing/index.blade.php`**: a "Cancel subscription" button appears next to the status badge whenever the user is on a paid plan (active or pending), with a `confirm()` dialog stating plainly that the move to Free is immediate and the current period isn't refunded — matching the confirm-dialog pattern already used for Disconnect/Revoke token/Delete account.
- [x] Tests (3 new in `BillingTest.php`): successful cancellation reverts plan/status/id correctly and asserts the real HTTP call shape sent to Cashfree; cancelling with no paid subscription is rejected with a flash error, not an abort; a Cashfree failure leaves the paid plan untouched (nothing half-applied).
- [x] `npm run build` succeeded; `php artisan test`: 137/137 passing (134 + 3 new); `vendor/bin/pint --test`: clean.
- [x] **Live-verified end-to-end against the real sandbox, independently on both sides**: created a real Cashfree sandbox subscription via `tinker`, set a throwaway account to it, cancelled it through the actual running app (`curl` through `/billing/cancel`, not the test client) — confirmed locally (`plan: free`, `cashfree_subscription_id: null`) *and* via a completely separate, direct `GET` straight to Cashfree's own subscription-status endpoint (not reusing `cancelSubscription()` — a genuinely independent check) that Cashfree's own record shows `CANCELLED`. Cleaned up afterward.
- **Next**: part 3, admin panel — owner said "we'll make" (deferred to a future session, not part of this one).

## Polish pass: filled the empty top-bar space, added a real guest-facing footer, de-hardcoded the Terms date

Owner circled the mostly-empty top header bar on a real screenshot and asked what could go there, plus asked for a footer with page links/description, plus flagged the Terms page's "Last updated" date as hand-typed.

- [x] **Top bar** ([layouts/app.blade.php](backend/resources/views/layouts/app.blade.php)): added the current page's title (`@yield('title', 'Dashboard')`, reused — it's already set by every page for the `<title>` tag) as a small muted label on the left, replacing what used to be empty space next to the mobile-only hamburger. Deliberately **not** a search box or notification bell — same reasoning as the earlier sidebar decision (`AskUserQuestion`, phase 2): a control that doesn't do anything yet is worse than no control. This is purely a wayfinding label, no new state or backend logic.
- [x] **Guest-facing footer** (new — guests previously had *zero* footer, unlike the logged-in sidebar's small tagline block): brand + a real one-paragraph product description, an Account column (Log in / Register), a Legal column (Terms of Service). Deliberately **not** duplicated inside the logged-in sidebar shell — the sidebar's nav already covers "all pages" for authenticated users, and a second full link-farm footer crammed into a 260px column would be clutter, not navigation. Reused the `.site-footer` dark-background CSS class from the very first design phase (removed when the sidebar rewrite replaced it with the sidebar's own footer block — re-added here since this is a second, genuinely different footer for a different audience).
- [x] **`body` gained `d-flex flex-column min-vh-100`** (previously only the logged-in `.app-shell` had its own flex context) so the new guest footer sticks to the bottom of the viewport on short pages (e.g. the login form) instead of floating directly under a half-empty page.
- [x] **Terms "Last updated" date no longer hand-typed prose in the Blade file**: `TermsController` now has a single `LAST_UPDATED` constant (a real date, not `now()`), passed to the view and formatted via Carbon. **Deliberately not `now()`** — a "last updated" date that always shows today is meaningless; the fix here is a single source of truth that only changes when someone deliberately bumps it (i.e., when the Terms content itself actually changes), not automatic dating.
- [x] `npm run build` succeeded; `php artisan test`: 137/137 passing, no test changes needed; `vendor/bin/pint --test`: clean.
- [x] Live-smoke-tested: confirmed in real served HTML that the guest footer's description/links render on `/login`, that the built CSS actually contains the `.site-footer` background rule (not just the class name sitting unstyled in HTML), that `/terms` renders the Carbon-formatted date correctly, and that a logged-in user's top bar shows the correct per-page title (checked on `/instances`, got "Instances" — not just assumed the `@yield` would resolve correctly). Cleaned up the throwaway account afterward.

## Follow-up (owner liked it): the footer is now on every page, not just guest ones

- [x] **Extracted `resources/views/partials/footer.blade.php`** — the footer used to be inlined once in the guest branch of `layouts/app.blade.php`. Pulled it into a shared partial *before* duplicating it a second time into the logged-in content column, specifically so the two copies can't drift apart later.
- [x] **Middle column is context-aware via `@auth`/`@else` inside the partial itself**: guests still see Account (Log in/Register — the only two pages that make sense for someone not logged in); logged-in users see Product (Dashboard, Instances, Billing, API Docs — effectively the same set as the sidebar, restated as real "all pages" footer links per the owner's original ask). The Legal column (Terms) is the same for everyone.
- [x] **Included at the bottom of the logged-in content column**, not full-bleed under the sidebar — the sidebar is a persistent full-height nav element; the footer belongs to the scrollable content area next to it, same as how the top-bar page title only applies there.
- [x] **Removed now-redundant content from the sidebar's own small footer block**: it previously duplicated a Terms link and copyright line that the new page-wide footer already covers reliably; trimmed it back to just the brand + tagline, and deleted the now-dead `.sidebar-copyright` CSS rule that styled the removed line — didn't leave unused CSS behind.
- [x] `npm run build` succeeded; `php artisan test`: 137/137 passing, zero test changes needed; `vendor/bin/pint --test`: clean.
- [x] Live-smoke-tested: confirmed in real served HTML that both `/dashboard` and `/billing` (two different pages, to prove it's actually shared via the layout and not copy-pasted per page) render the footer with real `href`s to Dashboard/Instances/Billing/API Docs/Terms, not just that a footer-shaped element exists.

## Test status

`php artisan test` (from `backend/`): **137 passed, 518 assertions**. `vendor/bin/pint --test`: clean.

`npm test` (from `whatsapp-worker/`, Vitest): **21 passed**. `npm run typecheck`: clean.

## Things to remember (learned the hard way)

**Workflow (from CLAUDE.md, the owner is a beginner):** one milestone per turn; inspect, explain, list files/commands and **wait for approval** before coding; after each milestone give what changed, commands, expected output and how to test, then **STOP**. The owner makes **all git commits** — never commit unless explicitly asked.

**`PROJECT_BRIEF.md` (the original brief) went unread for the entire session until the owner pointed to it** — everything through M0-M8 was correctly built from `CLAUDE.md` alone (which is derived from the brief and stayed faithful to it), but the brief had a whole payments section (real Cashfree sandbox credentials) that never made it into `CLAUDE.md` and so was invisible until then. Re-read `PROJECT_BRIEF.md` directly if anything about scope ever feels uncertain — don't assume `CLAUDE.md` captured 100% of it.

**PHP on this XAMPP install had no CA bundle configured** (`curl.cainfo`/`openssl.cafile` both empty, no `cacert.pem` anywhere) — broke every outbound HTTPS call from PHP (not just Cashfree) with `SSL certificate problem: self-signed certificate in certificate chain`, even though the system's own `curl.exe` worked fine (Windows trusts its OS cert store automatically; PHP's `curl` extension doesn't). Fixed by downloading the standard Mozilla bundle to `C:\xampp\php\extras\ssl\cacert.pem` and pointing both ini settings at it (backup: `C:\xampp\php\php.ini.pre-cacert-fix.bak`). `php artisan serve` must be restarted after any `php.ini` change to pick it up.

**Cashfree's own docs undersell two required details — verified by real sandbox calls, not assumption:**
1. `plan_details.plan_id` is required on every Create Subscription call, even ones that otherwise looked like the "send plan details inline" variant.
2. That `plan_id` isn't an arbitrary caller-chosen string — it must reference a Plan object that already exists at Cashfree (`POST /pg/plans` first), or the subscription call fails with `"Plan does not exist"`. `CashfreeClient::ensurePlanExists()` now handles this automatically (idempotent) before every subscribe attempt.

**Tests use a separate database.**
- `phpunit.xml` points at MySQL database `whatsapp_gateway_test`. It was created empty (tables come from migrations, not by hand). Tests wipe it on every run.
- Test classes that use `RefreshDatabase` include a `beforeRefreshingDatabase()` guard that aborts unless the DB name is `whatsapp_gateway_test`. **Copy this guard into every new DB test class.**
- Why: the default SQLite in-memory setup can't work here (PHP has no `pdo_sqlite`; we don't touch `php.ini`) and CLAUDE.md says MySQL only.
- Feature tests that render pages call `$this->withoutVite()`.

**The real database `whatsapp_gateway` contains a real user** (Prachi Barot, id 2) from the owner's own testing. Never wipe it. When smoke-testing against the real DB, use an `@example.invalid` email and delete exactly that row afterwards.

**Ports.** The owner usually has `php artisan serve` running on **8000**. For smoke tests, use **8001** and stop only the PID whose command line contains `127.0.0.1:8001` (the `artisan serve` PowerShell process spawns a child `php.exe -S`, so kill the port's owner, not just the launcher). The worker uses `127.0.0.1:3001`.

**Shared secret.** `backend/.env`'s `INTERNAL_API_SECRET` and `whatsapp-worker/.env`'s `INTERNAL_API_SECRET` must be the **exact same string** — that's the whole mechanism. If you ever regenerate one, copy it into the other too. WhatsApp auth files live in `C:\whatsapp-secrets\` (one folder per `instance_id`), outside the repo/`htdocs` on purpose — never move that under `backend/` or `whatsapp-worker/`.

**Webhook delivery (M8) is synchronous and best-effort, like the worker calls in M5-M7** — no queue, no retries. A slow or dead receiving endpoint adds latency to (but can't fail) the request that stores the incoming message, since `WebhookDispatcher` catches everything. `webhook_url` is also a straightforward SSRF surface (server-side request to an owner-supplied URL) — flagged in the class docblock, not solved; fine for this MVP's single-beginner-instance threat model, revisit before this is ever exposed more broadly.

**~~The worker's "connected" state is in-memory only~~ — FIXED in post-roadmap hardening (pending live verification).** Was: it did not survive the worker process restarting (including `tsx watch` auto-reloading on every file save during active development), so the database could keep saying `status = connected` long after the worker had forgotten it — any worker action (most obviously sending a message) then failed with 409/502 even though the page said "Connected". Hit this for real during M7 and M8 live-testing. Now: the worker calls `GET /internal/worker/sessions` once at boot and reconnects everything Laravel thinks should be live, automatically, using the saved credentials — no more manual Disconnect+Reconnect after every restart. If this ever regresses, the manual fix is still: click **Disconnect** then **Reconnect** on the instance page.

**Testing "incoming message" (M8) live has three separate, easy-to-hit false negatives — all correct behaviour, not bugs, but confusing until you know them:**
1. **Messaging the connected number FROM the connected number itself** (or from any of its own linked devices — including "Message Yourself" on the same phone) always looks like `fromMe: true` to Baileys, account-wide, not device-specific. It will never be treated as incoming. Must send from a genuinely different WhatsApp account.
2. **Sending into a group** that includes the number, instead of a direct 1-on-1 chat, arrives with `remoteJid` ending `@g.us` and is intentionally ignored (individual chats only, matches the send-side scope). Must open a private/direct chat with the exact number.
3. **Messages sent while the worker was offline/reconnecting** get delivered on reconnect as a backlog sync (`messages.upsert` `type: "append"`), not a live event (`type: "notify"`) — intentionally not forwarded, so as not to replay old history as if it just arrived. Wait until the instance page settles on "Connected" (a few seconds) before sending the test message.

`sessionManager.ts` now logs every `messages.upsert` (with `type` and count) and every forward/skip decision — added specifically because success was previously silent, which made "it's genuinely broken" indistinguishable from "it hasn't arrived yet" during live debugging. Check the worker terminal directly when this needs diagnosing again.

**Front-end.** Bootstrap comes in through `resources/css/app.css` (`@import 'bootstrap/dist/css/bootstrap.min.css'`) and `resources/js/app.js` (`import 'bootstrap'`). No Sass, no Tailwind. Run `npm run build` (or `npm run dev`) so pages get their CSS; `public/build` is git-ignored.

**Environment fixes done on 2026-09-21 (Windows/XAMPP), in case they come back:**
- *Apache + PHP curl:* `php_curl.dll` failed to load under Apache because Apache's older `apache\bin\libssh2.dll` (1.10.0) was found before PHP's (1.11.1). Fixed with one line in `C:\xampp\apache\conf\extra\httpd-xampp.conf`: `LoadFile "C:/xampp/php/libssh2.dll"` (backup: `httpd-xampp.conf.pre-libssh2fix.bak`). Apache must be restarted from the control panel for it to apply.
- *phpMyAdmin 5.2.3:* had no `config.inc.php` (the earlier edit went into `config.sample.inc.php`). Created `C:\xampp\phpMyAdmin\config.inc.php` (cookie auth, `127.0.0.1:3306`, `AllowNoPassword = true`, random `blowfish_secret`).
- *"MySQL shutdown unexpectedly" in the XAMPP panel:* caused by several `xampp-control.exe` windows open at once; a second panel starts a second `mysqld`, which fails on the locked `ibdata1`. Use only ONE control panel. MariaDB itself was fine.
- `apache\bin\libcurl.dll` is still renamed to `libcurl.dll.apache-backup` (it was never the cause; nothing enabled needs it). Optional: rename it back.
- Keep Apache **stopped** unless phpMyAdmin is needed (it serves `C:\xampp\htdocs`, including `.env` files).
- Untouched on purpose: MariaDB 10.4.32 config/port, OSGeo4W, `C:\xampp\php-8.2.12-backup`, `C:\xampp\phpMyAdmin-5.2.1-backup`.

## Next step

**Status as of 2026-09-23: everything through the account-settings/billing/queues work is committed** (`git log` confirms `76a0592` → `0b69621`, working tree clean). Owner chose to move straight to the **API Docs page** rather than working through the live-verification checklist first — that page is now built (see section above), coded/tested/live-smoke-tested, **not yet committed** (the owner makes all commits, per `CLAUDE.md`'s workflow rule).

Live-verification of the items below is still **unconfirmed** (not shown by git either way) — worth circling back to if anything in that area misbehaves:

1. **Queued webhook delivery**: needs `php artisan queue:work` running as its own process, or queued `DeliverWebhook` jobs just sit in the `jobs` table forever, never actually delivered. Verify by setting a webhook URL, receiving a message, and confirming delivery still happens (just asynchronously now).
2. **Change password / delete account**: `/account`, both forms.
3. Try hitting a plan limit for real: on the free plan, create 1 instance (the limit), try a 2nd — should be blocked with a clear message pointing at Billing.
4. **Stale-credentials fix**: click Reconnect on an instance whose status is `logged_out`/`disconnected` — confirm a genuinely fresh QR appears and scanning it reaches Connected again.
5. **Boot-reconnect fix**: once connected, restart the worker — confirm the instance goes back to "Connected" **on its own**.
6. **"Send a test message" button** and **password reset** (see `backend/storage/logs/laravel.log` for the reset link — `MAIL_MAILER=log`, nothing is actually emailed yet).

Open housekeeping items, still unresolved: a stray unused `backend/CLAUDE.md` (Aug 25, Laravel installer default) the owner may want deleted; billing has no self-serve downgrade/cancellation yet (upgrade-only); Cashfree's webhook confirmation is unverified (needs ngrok or real deployment).

**Now: API Docs page** (owner's explicit next step, filling the "API Docs — soon" navbar placeholder that's been sitting there since M3). Read `CLAUDE.md` §11 (mentions an "API docs/basic usage page" as one of the original UI pages), inspect what's already been explained informally in chat (the register→connect→token→send flow), and turn that into an actual page — present the plan (what it covers, code examples in which languages) before building, same as every other milestone. 2FA / audit logs / admin visibility across tenants remain explicitly deferred beyond that, owner's choice.
